# 09-scripts — small utilities

Automation scripts live here. Two rules:

1. Every script gets a comment header: what it does, how to run it, what it needs.
2. No secrets inside scripts — they read keys from outside the workspace.
